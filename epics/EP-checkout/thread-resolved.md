<!-- DERIVED — regenerated by `yad-timeline` from the thread's immutable epics. Do not hand-edit;
     it is not an artifact, it is the composed current-truth map AI/humans read for the next change. -->
# Resolved current truth — thread EP-checkout

The authoritative source for each artifact = the **latest epic in the thread that re-authored it**
(everything else inherits it by reference). This is what the next `yad-change` / `yad-epic` reads as
"what the feature actually is now", instead of the (superseded) genesis epic.

REPLACE artifacts resolve to a single owner (the latest re-author); ADDITIVE artifacts (`stories`,
`test-cases`) are the UNION of every contributor — a change-epic adds to the parent, it does not drop it.

| Artifact | Owning epic(s) | Why |
|----------|----------------|-----|
| `epic` | `EP-checkout` | never re-authored |
| `architecture` | `EP-checkout` | never re-authored |
| `contract` | `EP-checkout` | surface unchanged; the defect inherits it (pointer-lock) |
| `ui-design` | `EP-checkout` | never re-authored |
| `stories` | `EP-checkout` **+** `EP-checkout-queue-filter` | genesis S01–S05 inherited; the defect ADDS its regression story |
| `test-cases` | `EP-checkout` **+** `EP-checkout-queue-filter` | genesis cases inherited; the defect ADDS the missing negative case |

**Composed story set (6):** `EP-checkout-S01…S05` ← `EP-checkout`,
`EP-checkout-queue-filter-S01` ← `EP-checkout-queue-filter`. The parent stories are **not** dropped — the
defect re-authors only its own regression story and inherits the rest by reference.

- **Resolved contract-lock:** `sha256:3c8094c88f7d347910fb1bc623a3b42c61d57f986c789c7d5c1871573f025831`
  (genesis surface; never re-locked across this thread — no contract-surface change yet).
- **Thread tip:** `EP-checkout-queue-filter` (kind `defect`, sealed).
- **Open reconcile debt:** none.
