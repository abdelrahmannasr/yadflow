<!-- DERIVED — regenerated by `yad-timeline` from the thread's immutable epics. Do not hand-edit;
     it is not an artifact, it is the composed current-truth map AI/humans read for the next change. -->
# Resolved current truth — thread EP-istifta-inquiries

The authoritative source for each artifact = the **latest epic in the thread that re-authored it**
(everything else inherits it by reference). This is what the next `yad-change` / `yad-epic` reads as
"what the feature actually is now", instead of the (superseded) genesis epic.

| Artifact | Owning epic | Why |
|----------|-------------|-----|
| `epic` | `EP-istifta-inquiries` | never re-authored |
| `architecture` | `EP-istifta-inquiries` | never re-authored |
| `contract` | `EP-istifta-inquiries` | surface unchanged; the defect inherits it (pointer-lock) |
| `ui-design` | `EP-istifta-inquiries` | never re-authored |
| `stories` | **`EP-istifta-queue-filter`** | the defect re-authored the queue behaviour (regression story) |
| `test-cases` | **`EP-istifta-queue-filter`** | the defect added the missing negative case |

- **Resolved contract-lock:** `sha256:4abbbbc586846df8adf85054507bf11bdabf27fd6952a0bf7cd1d2804d0f3b71`
  (genesis surface; never re-locked across this thread — no contract-surface change yet).
- **Thread tip:** `EP-istifta-queue-filter` (kind `defect`, sealed).
- **Open reconcile debt:** none.
